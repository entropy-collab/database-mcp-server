# Build output
target/
!.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
.vscode/
.trae/
.cursor/
*.iml
*.ipr
*.iws
.project
.classpath
.settings/

# Documentation
docs/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Environment variables
.env*
!.env.example
# 生产配置文件（包含环境特定信息）
!application-production.yml

# Docker production compose (local overrides)
docker-compose.prod.yml

# Test coverage
coverage/
.jacoco/

# Docker image tar files (platform-specific builds)
*.tar
*.tar.gz

# Backup files
*.bak
*.backup

# Python
*.py
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Maven
.mvn/timestamp
pom.xml.tag
pom.xml.next
pom.xml.releaseBackup
release.properties
dependency-reduced-pom.xml
.flattened-pom.xml

# Java
*.class
*.jar
*.war
*.nar
*.ear
*.zip
*.rar
BOOT-INF/

# Build artifacts (platform-specific Docker files)
Dockerfile.*

# Test
jacoco.exec
jacoco-it.exec

# Temp
*.tmp
*.temp
.mtj.tmp/

# Crash logs
hs_err_pid*
replay_pid*
.attach_pid*

# Frontend（只读运维面板：Vite + React 19 + Meta Astryx）
# node_modules 约 111MB（@astryxdesign/core 解包后单独就有 20MB），不进版本库。
# node/ 是 frontend-maven-plugin 自己装的 node+npm 工具链，同理不进。
# 刻意放在 src/main/frontend/ 下而不是 target/ 下：放 target 的话每次 clean 都要重新联网下 node，
# 而 `mvn -o` 离线构建就再也过不去了。
#
# package-lock.json 刻意「不」忽略：Astryx 是 Beta，锁文件是"钉死版本"这件事的唯一保证。
# 缺了它 npm ci 直接失败，而退回 npm install 会现场解析出一份没人验证过的依赖树。
database-mcp-app/src/main/frontend/node_modules/
database-mcp-app/src/main/frontend/node/
database-mcp-app/src/main/frontend/dist/
database-mcp-app/src/main/frontend/.vite/

# Test data
test-mcp-*.json
code-smell-report.md
quality-check-report.md
$CSV
scripts/
